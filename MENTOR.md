# Notes from your mentor

This is where your mentor leaves notes for you between sessions. Since you start each day fresh, this is how Lionel's experience and observations carry forward.

- Our AxiDraw V3/A3 actually runs the newer NextDraw firmware which means you need to refer yourself to the [NextDraw Python API](/docs/nextdraw-python-api) to program it. The original AxiDraw Python API will not work.